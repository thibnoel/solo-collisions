/* 
 * This file has been automatically generated by the jrl-cmakemodules.
 * Please see https://github.com/jrl-umi3218/jrl-cmakemodules/blob/master/config.hh.cmake for details.
*/

#ifndef SOLO_COLLISIONS_CONFIG_HH
# define SOLO_COLLISIONS_CONFIG_HH

// Package version (header).
# define SOLO_COLLISIONS_VERSION_UNKNOWN_TAG 0 // Used to mention that the current version is unknown.
# define SOLO_COLLISIONS_VERSION "UNKNOWN-dirty"
# define SOLO_COLLISIONS_MAJOR_VERSION SOLO_COLLISIONS_VERSION_UNKNOWN_TAG
# define SOLO_COLLISIONS_MINOR_VERSION SOLO_COLLISIONS_VERSION_UNKNOWN_TAG
# define SOLO_COLLISIONS_PATCH_VERSION SOLO_COLLISIONS_VERSION_UNKNOWN_TAG

#define SOLO_COLLISIONS_VERSION_AT_LEAST(major, minor, patch) (SOLO_COLLISIONS_MAJOR_VERSION>major || (SOLO_COLLISIONS_MAJOR_VERSION>=major && \
                                                             (SOLO_COLLISIONS_MINOR_VERSION>minor || (SOLO_COLLISIONS_MINOR_VERSION>=minor && \
                                                                                                     SOLO_COLLISIONS_PATCH_VERSION>=patch))))

#define SOLO_COLLISIONS_VERSION_AT_MOST(major, minor, patch) (SOLO_COLLISIONS_MAJOR_VERSION<major || (SOLO_COLLISIONS_MAJOR_VERSION<=major && \
                                                            (SOLO_COLLISIONS_MINOR_VERSION<minor || (SOLO_COLLISIONS_MINOR_VERSION<=minor && \
                                                                                                     SOLO_COLLISIONS_PATCH_VERSION<=patch))))

// Handle portable symbol export.
// Defining manually which symbol should be exported is required
// under Windows whether MinGW or MSVC is used.
//
// The headers then have to be able to work in two different modes:
// - dllexport when one is building the library,
// - dllimport for clients using the library.
//
// On Linux, set the visibility accordingly. If C++ symbol visibility
// is handled by the compiler, see: http://gcc.gnu.org/wiki/Visibility
# if defined _WIN32 || defined __CYGWIN__
// On Microsoft Windows, use dllimport and dllexport to tag symbols.
#  define SOLO_COLLISIONS_DLLIMPORT __declspec(dllimport)
#  define SOLO_COLLISIONS_DLLEXPORT __declspec(dllexport)
#  define SOLO_COLLISIONS_DLLLOCAL
# else
// On Linux, for GCC >= 4, tag symbols using GCC extension.
#  if __GNUC__ >= 4
#   define SOLO_COLLISIONS_DLLIMPORT __attribute__ ((visibility("default")))
#   define SOLO_COLLISIONS_DLLEXPORT __attribute__ ((visibility("default")))
#   define SOLO_COLLISIONS_DLLLOCAL  __attribute__ ((visibility("hidden")))
#  else
// Otherwise (GCC < 4 or another compiler is used), export everything.
#   define SOLO_COLLISIONS_DLLIMPORT
#   define SOLO_COLLISIONS_DLLEXPORT
#   define SOLO_COLLISIONS_DLLLOCAL
#  endif // __GNUC__ >= 4
# endif // defined _WIN32 || defined __CYGWIN__

# ifdef SOLO_COLLISIONS_STATIC
// If one is using the library statically, get rid of
// extra information.
#  define SOLO_COLLISIONS_DLLAPI
#  define SOLO_COLLISIONS_LOCAL
# else
// Depending on whether one is building or using the
// library define DLLAPI to import or export.
#  ifdef solo_collisions_EXPORTS
#   define SOLO_COLLISIONS_DLLAPI SOLO_COLLISIONS_DLLEXPORT
#  else
#   define SOLO_COLLISIONS_DLLAPI SOLO_COLLISIONS_DLLIMPORT
#  endif // SOLO_COLLISIONS_EXPORTS
#  define SOLO_COLLISIONS_LOCAL SOLO_COLLISIONS_DLLLOCAL
# endif // SOLO_COLLISIONS_STATIC
#endif //! SOLO_COLLISIONS_CONFIG_HH
